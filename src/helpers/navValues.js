
/*
  Will contain all component compositions (menu items)
  for our application
*/
const navValues = {
    houselist: "HouseList", // where HouseList is rendered
    housedetail: "HouseDetail" // where HouseDetail is rendered
}

export default navValues;